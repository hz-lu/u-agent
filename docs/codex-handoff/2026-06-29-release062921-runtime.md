# Codex Handoff

## 总体目标
基于已开发的 U 盘便捷版 OpenClaw 实现集成 Hermes，集成实现：零安装、零痕迹、三平台原生、Universal 包、自我成长、多平台接入、定时自动化、子代理委派、沙箱隔离、可视化配置中心。所有 Hermes 能力需要和现有 OpenClaw 前端界面无缝融合，前端操作要给用户良好体验。

## 当前目标
将 F 盘现有完整 runtime 抽取并复制到正式初始化 release 目录 `D:\share\1产品\openclaw\release062921\runtime`，形成可拷贝到 U 盘的完整 Windows 便携运行目录。

## 已完成
- 已从 `F:\runtime` 完整复制 runtime 到 `D:\share\1产品\openclaw\release062921\runtime`。
- 使用 `robocopy /E /COPY:DAT /DCOPY:DAT /R:2 /W:2 /MT:16 /XJ` 执行大目录复制。
- runtime 源/目标对账一致：`67222` 个文件，`1486169466` bytes，约 `1.384GB`。
- release 总体大小约 `1.669GB`，总文件数 `68100`。
- 已确认关键文件存在：`runtime\node.exe`、`runtime\openclaw.cmd`、`runtime\openclaw.zip`、`runtime\node_modules\openclaw`、`runtime\python3\python.exe`、`runtime\HermesPortable\venv`。
- 已确认 Node/Python 可执行：`node v24.15.0`、`Python 3.12.13`、Hermes portable Node `v24.15.0`。
- 已更新 `RELEASE-MANIFEST.json`，标记 `runtimeIncluded: true`。
- 已更新 `README-PORTABLE.md`，说明 runtime 已包含。

## 改动文件
- `D:\share\1产品\openclaw\release062921\runtime\...`
- `D:\share\1产品\openclaw\release062921\README-PORTABLE.md`
- `D:\share\1产品\openclaw\release062921\RELEASE-MANIFEST.json`
- `docs/codex-handoff/2026-06-29-release062921-runtime.md`

## 关键决策
- runtime 按完整目录复制，不清理 `__pycache__` 或测试文件，避免破坏运行闭环。
- release 的 `data/` 仍保持初始化模板，不复制 F 盘用户态数据。
- 微信插件已在上一阶段复制到 `extensions/openclaw-weixin`，本阶段未复制微信账号状态。
- 精确敏感文件扫描中仅发现第三方依赖 `bottleneck\.env`，内容为本地 Redis 默认配置，不是用户密钥。

## 待继续
- 如需分发，可以下一步将 `D:\share\1产品\openclaw\release062921` 打包成 zip。
- 可在一块干净 U 盘根目录拷贝该目录内容后做一次真实启动验证。

## 验证结果
- `robocopy` 返回 `ROBOCOPY_EXIT=1`，表示成功复制且无失败。
- 源/目标 runtime 文件数和大小完全一致。
- release 关键文件存在性检查通过。
- release 内主进程、preload、renderer 语法检查此前已通过；本阶段未改应用代码。
- `README-PORTABLE.md` 用 UTF-8 读取确认中文正常。

## 如果需要下一台 Codex 接手，提示词
请在 `D:\github\u-agent` 继续开发 U 盘便携版 OpenClawPro/Hermes 项目。当前正式 release 目录是 `D:\share\1产品\openclaw\release062921`，已包含 `win-unpacked`、完整 `runtime`、初始化 `data`、`skills`、`extensions/openclaw-weixin`。不要从 F 盘复制 `data` 用户态内容。下一步如果用户要求正式分发包，请基于该 release 目录打 zip，并再次检查不包含 `.license`、API Key、聊天历史、Hermes 记忆、微信账号登录状态。
